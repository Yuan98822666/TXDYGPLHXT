graph TD
    A[开始快照周期] --> B[生成 kz_no + market_time]
    B --> C[并发采集概念板块 + 行业板块]
    C --> D[提取所有点名股 secid 列表（去重）]
    D --> E[并发请求个股快照接口]
    E --> F[构建 RawBlockHuoyue + RawStockHuoyue 对象]
    F --> G[批量事务写入数据库]
